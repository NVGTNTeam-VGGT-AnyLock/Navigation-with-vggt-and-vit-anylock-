<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ── Section: Navigation Modes ──────────────────────────────── -->
    <item
        android:id="@+id/nav_modes"
        android:title="@string/drawer_section_modes">
        <menu>
            <item
                android:id="@+id/transportFragment"
                android:icon="@drawable/ic_car"
                android:title="@string/tab_transport" />
            <item
                android:id="@+id/pedestrianFragment"
                android:icon="@drawable/ic_walk"
                android:title="@string/tab_pedestrian" />
        </menu>
    </item>

    <!-- ── Section: Management ────────────────────────────────────── -->
    <item
        android:id="@+id/nav_manage"
        android:title="@string/drawer_section_manage">
        <menu>
            <item
                android:id="@+id/mapFragment"
                android:icon="@drawable/ic_map"
                android:title="@string/tab_map" />
            <item
                android:id="@+id/routeBuilderFragment"
                android:icon="@drawable/ic_route"
                android:title="@string/tab_routes" />
            <item
                android:id="@+id/addLocationFragment"
                android:icon="@drawable/ic_add"
                android:title="@string/tab_add" />
        </menu>
    </item>

    <!-- ── Section: Insights ──────────────────────────────────────── -->
    <item
        android:id="@+id/nav_insights"
        android:title="@string/drawer_section_insights">
        <menu>
            <item
                android:id="@+id/analyticsFragment"
                android:icon="@drawable/ic_analytics"
                android:title="@string/tab_analytics" />
            <item
                android:id="@+id/visualSearchFragment"
                android:icon="@drawable/ic_search_photo"
                android:title="@string/tab_visual_search" />
        </menu>
    </item>

</menu>
