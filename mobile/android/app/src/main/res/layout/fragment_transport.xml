<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:keepScreenOn="true">

    <!-- ── Google Map (full-screen background) ──────────────────────── -->
    <fragment
        android:id="@+id/map_container"
        android:name="com.google.android.gms.maps.SupportMapFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- ── PiP Camera Preview (Picture-in-Picture, bottom‑right) ──── -->
    <androidx.camera.view.PreviewView
        android:id="@+id/pip_preview_view"
        android:layout_width="160dp"
        android:layout_height="120dp"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        android:scaleType="centerCrop"
        app:cornerRadius="12dp"
        android:elevation="8dp" />

    <!-- ── Stale Indicator (top‑center overlay) ─────────────────────── -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/card_stale_indicator"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|center_horizontal"
        android:layout_marginTop="48dp"
        android:visibility="gone"
        app:cardElevation="6dp"
        app:cardCornerRadius="16dp"
        app:strokeWidth="0dp"
        android:paddingHorizontal="16dp"
        android:paddingVertical="8dp"
        android:backgroundTint="#E6FF5252">

        <TextView
            android:id="@+id/tv_stale_warning"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/transport_stale_warning"
            android:textColor="@android:color/white"
            android:textAppearance="?attr/textAppearanceLabelSmall"
            android:textStyle="bold" />

    </com.google.android.material.card.MaterialCardView>

    <!-- ── Scanning Spinner (center) ────────────────────────────────── -->
    <com.google.android.material.progressindicator.CircularProgressIndicator
        android:id="@+id/progress_scanning"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="center"
        android:visibility="gone"
        android:indeterminate="true"
        app:indicatorSize="48dp"
        app:trackThickness="4dp" />

    <!-- ── Status Bar (bottom, over map) ────────────────────────────── -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/card_status"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="80dp"
        app:cardElevation="6dp"
        app:cardCornerRadius="16dp"
        app:strokeWidth="0dp"
        android:paddingHorizontal="16dp"
        android:paddingVertical="8dp"
        android:backgroundTint="#DD1A1C1E">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/tv_bearing"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/transport_bearing_unknown"
                android:textColor="@color/white"
                android:textAppearance="?attr/textAppearanceLabelSmall"
                android:layout_marginEnd="12dp" />

            <TextView
                android:id="@+id/tv_position"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/transport_position_unknown"
                android:textColor="@color/white"
                android:textAppearance="?attr/textAppearanceLabelSmall" />

        </LinearLayout>

    </com.google.android.material.card.MaterialCardView>

    <!-- ── Mock Button (top‑right, development only) ───────────────── -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_mock_khreshchatyk"
        style="@style/Widget.Material3.Button.TonalButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:layout_marginTop="48dp"
        android:layout_marginEnd="12dp"
        android:text="@string/mock_khreshchatyk"
        android:textAppearance="?attr/textAppearanceLabelSmall"
        android:textColor="@color/white"
        android:backgroundTint="#CCFF5722"
        app:cornerRadius="20dp"
        android:paddingHorizontal="12dp"
        android:paddingVertical="6dp"
        android:elevation="6dp"
        android:visibility="visible" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
