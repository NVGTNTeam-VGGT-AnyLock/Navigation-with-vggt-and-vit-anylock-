<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:keepScreenOn="true">

    <!-- ── Camera Preview (full‑screen) ─────────────────────────────── -->
    <androidx.camera.view.PreviewView
        android:id="@+id/camera_preview_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop" />

    <!-- ── Scan Result Card (floating, semi‑transparent, top) ─────── -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/card_scan_result"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:layout_marginTop="48dp"
        android:visibility="gone"
        app:cardElevation="8dp"
        app:cardCornerRadius="16dp"
        app:strokeWidth="0dp"
        android:backgroundTint="#DD1A1C1E">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <TextView
                android:id="@+id/tv_result_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/pedestrian_scan_result"
                android:textAppearance="?attr/textAppearanceTitleSmall"
                android:textColor="@color/white"
                android:layout_marginBottom="8dp" />

            <TextView
                android:id="@+id/tv_result_coords"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/pedestrian_position_unknown"
                android:textAppearance="?attr/textAppearanceBodySmall"
                android:textColor="@color/chart_teal"
                android:layout_marginBottom="4dp" />

            <TextView
                android:id="@+id/tv_result_bearing"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/pedestrian_bearing_unknown"
                android:textAppearance="?attr/textAppearanceBodySmall"
                android:textColor="@color/chart_amber" />

        </LinearLayout>

    </com.google.android.material.card.MaterialCardView>

    <!-- ── Scanning Overlay (shown during burst capture + API calls) ─ -->
    <FrameLayout
        android:id="@+id/overlay_scanning"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#99000000"
        android:visibility="gone">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:orientation="vertical"
            android:gravity="center">

            <com.google.android.material.progressindicator.CircularProgressIndicator
                android:id="@+id/progress_scanning"
                android:layout_width="56dp"
                android:layout_height="56dp"
                android:indeterminate="true"
                app:indicatorSize="56dp"
                app:trackThickness="5dp"
                android:layout_marginBottom="16dp" />

            <TextView
                android:id="@+id/tv_scanning_status"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/pedestrian_scanning"
                android:textAppearance="?attr/textAppearanceTitleMedium"
                android:textColor="@color/white" />

        </LinearLayout>

    </FrameLayout>

    <!-- ── Locate Me Button (bottom‑center, large FAB) ────────────── -->
    <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
        android:id="@+id/fab_locate_me"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="32dp"
        android:text="@string/pedestrian_locate_me"
        android:icon="@drawable/ic_camera_capture"
        app:iconSize="24dp"
        app:iconGravity="textStart"
        android:textAppearance="?attr/textAppearanceTitleSmall" />

    <!-- ── Countdown Text (on the FAB during burst) ────────────────── -->
    <TextView
        android:id="@+id/tv_countdown"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="96dp"
        android:text=""
        android:visibility="gone"
        android:textAppearance="?attr/textAppearanceDisplayMedium"
        android:textColor="@color/white"
        android:textStyle="bold"
        android:shadowColor="#FF000000"
        android:shadowRadius="4"
        android:shadowDx="0"
        android:shadowDy="0" />

    <!-- ── Mock Button (top‑right, development only) ───────────────── -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_mock_khreshchatyk"
        style="@style/Widget.Material3.Button.TonalButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:layout_marginTop="48dp"
        android:layout_marginEnd="12dp"
        android:text="@string/mock_khreshchatyk"
        android:textAppearance="?attr/textAppearanceLabelSmall"
        android:textColor="@color/white"
        android:backgroundTint="#CCFF5722"
        app:cornerRadius="20dp"
        android:paddingHorizontal="12dp"
        android:paddingVertical="6dp"
        android:elevation="6dp"
        android:visibility="visible" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
